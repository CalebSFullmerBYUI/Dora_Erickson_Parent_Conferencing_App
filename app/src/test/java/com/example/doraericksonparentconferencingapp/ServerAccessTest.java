package com.example.doraericksonparentconferencingapp;

import org.junit.Test;

import static org.junit.Assert.*;

/**
 * Tests to ensure methods connecting to server function correctly.
 *
 * @see <a href="http://d.android.com/tools/testing">Testing documentation</a>
 */
public class ServerAccessTest {
    /**
     * Tests testLoginInfo() to make sure it returns a valid response from the server
     * and handles bad responses.
     */
    @Test
    public void loginInfoWorks() {
        //TestLoginInfo();
    }
}